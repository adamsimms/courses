{
  "name": "syllabi",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-og-square.mjs && node scripts/build-all.mjs",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "hugo-extended": "0.164.0"
  }
}
